# Agent Instructions

This vault is a human-readable handoff layer. Start from [README.md](README.md), then follow the protocol for the task type.

The human-facing queue/dashboard is [DASHBOARD.md](DASHBOARD.md).

For delegated work, follow [protocols/delegate.md](protocols/delegate.md). Do not treat this file as the source of truth; it is only a compatibility entrypoint for agents that automatically read `AGENTS.md`.
